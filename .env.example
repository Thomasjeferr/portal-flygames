# ===========================================
# Portal Futvar / Fly Games - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha os valores.
# NUNCA commite o .env com credenciais reais.
# ===========================================

# ----- OBRIGATÓRIAS -----

# URL do banco de dados (Prisma)
# SQLite (desenvolvimento): file:./dev.db
# PostgreSQL (produção): postgresql://user:password@host:5432/dbname
DATABASE_URL="file:./dev.db"

# API Key do Resend (e-mails transacionais)
# Obter em: https://resend.com/api-keys
RESEND_API_KEY="re_xxxxxxxxxxxx"

# ----- CLOUDFLARE STREAM (vídeos) -----
# Necessário para importar/gerar URLs assinadas de vídeos
# Obter em: https://dash.cloudflare.com > Stream > API

CLOUDFLARE_ACCOUNT_ID="seu_account_id"
CLOUDFLARE_STREAM_API_TOKEN="seu_token_api"
# Código do customer (ex: oi7m1j64ce055sre) - sem o domínio
CLOUDFLARE_STREAM_CUSTOMER_CODE="seu_customer_code"

# ----- URL DO APP -----
# Usado em links de e-mail, verificação, reset de senha
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ----- PAGAMENTOS -----
# Podem ser configurados via Admin > APIs de Pagamento (salvos no banco)
# ou via variáveis de ambiente (prioridade)

# Woovi (Pix) - https://woovi.com
WOOVI_API_KEY=""
WOOVI_WEBHOOK_SECRET=""

# Stripe (cartão) - https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
# Chave publicável (pk_test_... ou pk_live_...) - usada no frontend com Stripe Elements
# STRIPE_PUBLISHABLE_KEY é salva no banco via Admin

# ----- OPCIONAIS -----

# Vercel Blob (upload de imagens em produção)
# Na Vercel: Storage > Create Database/Blob > conecte ao projeto.
# A variável BLOB_READ_WRITE_TOKEN é criada automaticamente.
# Sem ela, uploads no admin (logo de e-mail, thumbnails, etc.) falham em produção.

# Salt para hash de IP em analytics (privacidade)
# Gere um valor aleatório em produção
GEO_SALT="portal-futvar"

# Seeds (apenas para desenvolvimento)
# Usados em: npm run db:seed
# ADMIN_EMAIL="admin@portal.com"
# ADMIN_PASSWORD="Admin@123"
# CLIENT_EMAIL="cliente@teste.com"
# CLIENT_PASSWORD="Cliente@123"
